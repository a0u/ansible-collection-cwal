---

- name: 'set loader.conf(5) variables'
  sysrc:
    state: 'present'
    conf: '{{ loader | mandatory }}'
    file: '/boot/loader.conf'
  when: 'ansible_facts["distribution"] == "FreeBSD" and loader is defined'
  tags: 'loader'

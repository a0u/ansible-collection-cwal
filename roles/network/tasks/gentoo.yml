---

- name: 'create netifrc configuration'
  template:
    dest: '/etc/conf.d/net'
    src: 'gentoo/net.j2'
    owner: 'root'
    group: 'root'
    mode: 0444
  when: '(network | length > 0) or (network_extra | length > 0)'
  tags: 'network'
